# HEAD RULINGS — amendments to SPEC.md (these override SPEC where they conflict)

Reviewed by the head 2026-09-09 after reading SPEC.md in full. SPEC is APPROVED with the following amendments.
Builders read SPEC.md first, then this file. Where they conflict, THIS FILE WINS.

## R1. Tier-0 `assistant_frame_leak` is narrowed to true self-identification / drafting frames
The rule bypasses every gate and is the only road to `likely_llm`, so it must be near-100% precision. Several of SPEC §B.1's patterns are ordinary human chat ("Sure, sounds good!", "let me know if you need anything", "hope this helps", "size nasıl yardımcı olabilirim" is what a human receptionist says). RULING:
- KEEP as rule patterns: `as an AI`, `I'm an AI`, `I am an AI`, `language model`, `my (training data|knowledge cutoff)`, `I (don't|do not) have (access to )?real-time`, `I cannot browse`, `here('s| is) (a|an|the) (draft|revised|rewritten|polished)`, `as of my (last|latest) (update|training)`; TR: `bir yapay zeka (modeli|asistanı)(yım)?`, `yapay zeka dil modeli`, `bilgi kesim tarih`; AR: `كنموذج ذكاء اصطناعي`, `نموذج لغوي`, `بصفتي ذكاء اصطناعي`.
- MOVE to `llm_lexicon_strong` (weight per SPEC, NOT a rule): `^(sure|certainly|absolutely|of course)[,!]`, `I hope (this|that) helps`, `let me know if you (need|have|'d like)`, `would you like me to`, `feel free to (ask|reach out)`, `umarım (bu )?(bilgiler )?yardımcı ol(ur|muştur)`, `başka bir sorunuz olursa`, `size nasıl yardımcı olabilirim`, `نأمل أن (تكون|يكون)`, `هل تود مني`, `لا تتردد في التواصل`. Tag the support-desk ones `domain:'cs'` so `--domain customer_service` zeroes them.
- The quotation/"talking about chatgpt" suppression from SPEC still applies to the rule.

## R2. `markdown_in_chat` — plain dash bullets alone are NOT a rule hit
A human on WhatsApp writes "- 2 yetişkin\n- 1 çocuk" all the time. RULING: the rule fires only on (a) a markdown header `^#{1,6} `, or (b) DOUBLE-asterisk bold, or (c) a fenced code block, or (d) a pipe table, or (e) ≥2 bullet lines AND at least one bullet with a bold lead-in (`- **X:**`). Plain bullet lines alone feed the weak `colon_led_list`/structure signals, never the rule.

## R3. `near_duplicate` proves "not independently authored", not "LLM"
A near-duplicate across senders is template/copy/spam evidence. RULING: it stays a Tier-0 rule (high precision for its own claim) but ALONE it drives the final verdict to `leaning_llm` with `warnings:['templated_or_copied']` and a report line "duplicate of <id> at Jaccard J — template or copy; not proof of LLM authorship". `likely_llm` via near_duplicate requires a SECOND Tier-0 rule.

## R4. Own-bot marker and reference-code formats (verified in the repo, not assumed)
- Marker regex: `<the product's bot-marker pattern — kept out of this public repo; configure it locally via markers.json>` (lesson kept: a FALLBACK variant of the marker exists in that product and the pattern must cover it).
- Reference codes are a three-letter prefix + hyphen + 6 ASCII digits. A customer may retype with Arabic-Indic digits and may drop the hyphen; the pattern (kept out of this public repo) uses `[0-9٠-٩]{5,7}` with an optional hyphen. Never `\d`.

## R5. `detect()` stays pure — the expiry clock comes from the caller
SPEC §D.4 calls `Date.now()` inside scoring while §C.6 says `detect` is pure. RULING: `detect(text, opts)` takes `opts.now` (ms epoch, optional). The CLI passes `Date.now()`; the library default is `now = undefined` ⇒ expiry check skipped and `warnings` gets `expiry_not_checked`. Determinism test runs with a fixed `now`.

## R6. `--domain` default stays `general`
The agent passes `--domain customer_service` only when the caller states the text comes from a support desk / agency staff. README lists failure mode #4 verbatim. No auto-detection of domain this round.

## R7. Customer-service snippet library — B2 authors it
No such file exists in the repo. B2 writes `eval/fixtures/cs-snippets.jsonl`: 30 rows (10 EN / 10 TR / 10 AR) of realistic human support-desk phrases (hotel/travel agency), used as negative control (e). Any row scoring above τ goes on the domain-suppression list in the eval report; B1 is told via the head, not edited by B2.

## R8. PII in committed fixtures — hard rules
- `human-chat.jsonl` is NOT committed. `make-splits.mjs` generates it under `eval/data/` (gitignored) from the pulled corpus at eval time.
- Corpus-derived rows in COMMITTED fixtures (`judge-tests.jsonl`, `must-not-fire.jsonl`) are allowed ONLY from writer R0 (the repository owner's own messages). No R1/R2 rows, no persona rows containing names.
- Every committed row that came from the corpus passes a deny-by-default scrub: any capitalized/proper token that is not in an allowlist (cities, hotel-catalogue words, months, weekdays, room types, function words) becomes `[NAME]`; any product reference code becomes `[REF]`; any digit run ≥7 becomes `[NUM]`. Guest-name-shaped tokens after `isim/isimler/adı/names/الأسماء` are ALWAYS `[NAME]`.
- The head's acceptance grep for phone-shaped digit runs applies to every tracked file under tools/ and .claude/agents/.

## R9. Public-dataset caps (no silent caps)
`fetch-public-datasets.mjs` pulls at most 1,500 rows PER LABEL PER DATASET (MAGE and Fake-Reviews included), pages `/rows` with `length=100`, records the cap and the actual count in `manifest.json`, and prints "capped at N of M" to stderr. Total download must stay under ~60 MB. One attempt at ALHD (Zenodo record 17249602) is permitted: HEAD the file list first; skip and record "skipped: size/unavailable" if any file exceeds 50 MB or the record is not directly downloadable.

## R10. No second generator, no API spend
Hard-mode and humanized fixtures are authored in-session by the builder (Claude-written text IS LLM text). Provenance is recorded per row. This is the substitute for SPEC's "commission a second generator" and it is final.

## R11. Shipped default weights
B1 ships `weights.v1.json` (prior). CAL produces `weights.fitted.json`. The CLI default remains the prior file this round; the head decides after reading the eval report whether the fitted file becomes the default. Nobody flips it unilaterally.

## R12. Determinism across Node versions
Only Node v24.5.0 is installed. `selftest.mjs` asserts byte-identical output across two runs in the same process and across two separate processes; the "two Node minor versions" clause is recorded as NOT VERIFIED in the selftest output, not silently dropped.

## R13. Agent-file model line
`model: opus` — owner ruling 2026-09-03. Not fable, not sonnet, not haiku.

## R14. Sibling-safety for builders (the fleet shares one repo)
- Touch ONLY the files SPEC §I assigns to you. Never `git stash`, `git checkout`, `git add`, `git commit`, `git reset`, never edit `.gitignore` except B2's single line, never touch backend/ or frontend/ or CLAUDE.md.
- Scratch files go under the session scratchpad, never the repo root, never /tmp.
- Do not run jest, gate.sh, live tests or any paid API. `fetch-public-datasets.mjs` is the only file that touches the network and only `datasets-server.huggingface.co` (+ the one ALHD Zenodo attempt in R9).
- The DB read in `pull-messages-corpus.cjs` is a READ; the head has approved one execution by the CAL lane. Builders do not run it.

## R15. Build priority inside B1 (if context runs short, ship in this order)
1. §B.9 pipeline + language ID + segmenter + tokenizer with the Unicode assertions;
2. Tier-0 rules (as amended by R1–R4) and the gates/caps;
3. Tier-1 features, lexicons via build-lexicon.mjs, Aho-Corasick;
4. scorer (two channels, 2-D table, ≥2-features-from-≥2-groups invariant, contribution-sum invariant);
5. CLI flags incl. `--jsonl`, `--aggregate`, `--corpus`, exit codes;
6. selftest.mjs.
Ship a smaller CORRECT tool over a larger broken one; say exactly what was cut.

# PART 2 — OWNER CLARIFICATION 2026-09-09: THIS IS NOT A FEATURE OF THE OWNER'S OTHER PRODUCT

The owner: "this is not related with the other project". Everything below OVERRIDES SPEC.md and Part 1 wherever they assume that product's repo or traffic.

## R16. Location: standalone project `~/Desktop/llm-detect` (its own git repo) + a GLOBAL agent
Nothing is written inside the other project's directory — not a file, not a .gitignore line, not a scratch probe. Layout of the standalone repo:
```
llm-detect/
  package.json                 { "name":"llm-detect", "type":"module", "private":true, "engines":{"node":">=20"},
                                 "bin":{"llm-detect":"./stylometry.mjs"}, "scripts":{"selftest":"node selftest.mjs","eval":"node eval/run-eval.mjs"} }  NO dependencies field.
  stylometry.mjs  (+ lib/*.mjs local modules allowed)
  lexicon-src/  build-lexicon.mjs  lexicon.v1.json  weights.v1.json  selftest.mjs
  markers.json                 (R17) user-configurable known-machine markers; ships as []
  RUBRIC.md  README.md  LICENSE (MIT, owner's name left as "<owner>")
  agent/llm-text-detector.md   source of truth for the agent (B3)
  install.sh                   copies agent/llm-text-detector.md to ~/.claude/agents/ and prints the LLM_DETECT_HOME line to add to the shell profile (B3)
  eval/  run-eval.mjs  fetch-public-datasets.mjs  make-splits.mjs  README-eval.md
         adapters/supabase-messages.cjs   (R18)
         fixtures/*.jsonl   data/ (gitignored, .gitkeep)   out/
  docs/design/                 the head copies SPEC/HEAD-RULINGS/R1 here after a PII grep (not a builder task)
  .gitignore                   eval/data/  node_modules/  .env  *.log  .DS_Store
```
`tools/llm-detect/...` paths in SPEC map to the repo root of this project. `.claude/agents/llm-text-detector.md` in SPEC maps to `agent/llm-text-detector.md` here, installed globally by install.sh.

## R17. `own_bot_marker` becomes the generic `known_machine_marker` rule
That product's marker and reference-code patterns are one product's markers and do NOT ship as defaults. The rule reads `markers.json`: an array of `{ "name": string, "pattern": string (regex source, u-flag applied), "note": string }`, default `[]` ⇒ the rule never fires. `--markers <path>` overrides. Behaviour when a marker matches is unchanged from SPEC §B.1 (`warnings:['pasted_machine_text']`, the report says the string is machine-written and the sender may be forwarding it). README documents the format with a GENERIC example (e.g. a booking reference like `\bREF-[0-9]{6}\b` and a bot signature line); R4's regexes appear nowhere in committed files.

## R18. The corpus pull becomes an optional adapter, with no product-specific paths in committed files
`eval/adapters/supabase-messages.cjs`: flags `--env <path to a .env holding SUPABASE_URL + SUPABASE_SERVICE_KEY>` (required), `--table <name>` (default `messages`), `--human-ids <comma list of user_phone values that are known humans>` (required; mapped to R0..Rn in input order), `--synthetic-prefix <string>` (default `999`), `--out <dir>` (must be gitignored — `git check-ignore -q` guard, exit 2 otherwise), `--dry-run`, `--i-have-approval`. Loads `dotenv` and `@supabase/supabase-js` via `NODE_PATH` (documented: `NODE_PATH=/path/to/some/node_modules node eval/adapters/supabase-messages.cjs …`); if they are not resolvable it prints a one-line instruction and exits 2. Everything else from SPEC §I B2 item 1 (writer_id/persona_id hashing, never a phone number) stands. The CAL lane receives the actual env path and ids on the command line; committed docs and REPORT.md show them as `<env>` and `<ids>`.

## R19. The agent is GLOBAL, so it locates the tool via `LLM_DETECT_HOME`
The agent file resolves the tool at `${LLM_DETECT_HOME:-$HOME/Desktop/llm-detect}` and runs `node "$LLM_DETECT_HOME/stylometry.mjs" …` and reads `"$LLM_DETECT_HOME/RUBRIC.md"`. If the directory or the module is missing it says so and judges alone, capped at `leaning_*` (SPEC's last anti-pattern). Generic wording throughout: replace "our own bot", "our marker", "a paying customer", "our confirmations" with "a configured known-machine marker (markers.json)", "the sender may be a person forwarding machine-written text". Keep the Arabic-transliteration rule (the owner's terminal reverses RTL — that is about the owner, not the product). `model: opus` stands.

## R20. Generic framing in README and RUBRIC
The tool is for reviews, chat messages, emails and essays in EN/TR/AR. No "this product", "our traffic", "our customers". The in-house corpus facts stay because they are the only chat-length measurement that exists, described as: "a private WhatsApp customer-chat corpus with three human writers, used for calibration and never distributed". The verbatim §G.2 caveats keep their numbers. `--channel whatsapp` and `--domain customer_service` stay as generic options.

## R21. What the previous (stopped) build round left behind
B3 had written a first agent file; it is at `<scratchpad>/build-prev/llm-text-detector.md` — B3 may start from it but must apply R17/R19. Nothing else survived. Any `tools/llm-detect` partial under `<scratchpad>/build-prev/tools-partial` is reference only.

## R22. ARABIC IS OUT OF SCOPE (owner, 2026-09-09: "there is no arabic")
Language scope for this project is **English + Turkish**. This overrides every "EN/TR/AR" and "Arabic first" line in SPEC.md, Part 1 and Part 2, and in the lane documents.
- Do NOT build: `ar_dialect_markers`, `ar_orthographic_shortcuts`, `arabizi`, `ar_tashkeel_band`, `ar_tatweel`, the AR lexicons (`lexicon-src/ar.txt`, `human-ar.txt`, `arabizi.txt`), the AR rows of every shared lexicon/regex (assistant_frame_leak AR patterns, AR openers/closers/hedges/politeness), `llm-ar.jsonl`, the two KFUPM Arabic datasets, the Arabic rubric criteria, the Arabic reporting rule, the Arabic caveat texts.
- Weight cells become `{en,tr} × {chat,prose}` (four, not six).
- Language ID still recognises Arabic script — only to return `language.primary = "unsupported"` and `insufficient_text` with `gates.failed: ["G3_lang"]` and `reason: "unsupported_language"`. Never score it.
- The private corpus: rows whose dominant script is Arabic (writer R1, many personas) are EXCLUDED from calibration by make-splits.mjs (script filter, reported as a count). Leave-one-writer-out therefore runs over R0 and R2 only; say so in the report.
- `must-not-fire.jsonl`: drop the Arabic rows; the gate applies to the rows that remain (report the counts).
- The Unicode facts of SPEC §B.9 that concern Arabic (AR_LETTER, tatweel in WORD_RE, `\p{Nd}` for Arabic-Indic digits) stay in the tokenizer/selftest because they protect script detection and the no-`\d` rule; they are not features.
- Any SPEC or lane text about Arabic that is not listed here is reference material, not a deliverable.

# PART 3 — BUILD-ROUND RULINGS 2026-09-09 (after B1/B2/B3 + CAL, before the verify round closed)

## R23. `eval/out/weights.fitted.json` does NOT become the default — `weights.v1.json` (prior) stays
Measured, not hoped: the fitted file covers only two of the four cells (`en:prose`, `tr:chat`); `en:chat` and `tr:prose` say "not fitted — too few rows survived the gates". The fairness-constrained threshold could not be evaluated on the non-native or formal-register strata in either fitted cell (fewer than 20 human rows each), so the R11 condition "passes the fairness-constrained thresholds in both languages" is not met — it is not measurable. `tr:chat`'s fitted t is 1.000 (flags only rows the isotonic map pinned at certainty) and its hard-mode AUC is 0.450, below chance. Eight fitted coefficients in `en:prose` carry the opposite sign to §B (including `terminal_punct_ratio` at −0.175), which SPEC §D.5 step 5 says to investigate, not ship. RULING: the CLI default remains the prior file; `--weights eval/out/weights.fitted.json` stays available for the caller who wants the fitted `en:prose` cell and accepts the report's caveats. Re-decide when a cell has ≥100 human rows per length bucket on the test side AND ≥20 rows in each fairness stratum.

## R24. Register-proxy LLM evidence cannot carry `leaning_llm` on its own (arbitration of fixture A7, revised after the refuter)
Fixture `must-not-fire` A7 (a polite, correctly spelled Turkish guest booking message, 45 tokens, WhatsApp) came out `leaning_llm` on `terminal_punct_ratio` (z 2.17 — 59 % of the LLM channel on its own), `sentence_initial_caps`, `greeting_signoff_frame`, `politeness_formula`, with a human channel of exactly 0 because §B.4 deliberately gives correct orthography weight zero. CAL showed it is not one fixture (three of the 30 support-desk lines lean LLM at `--domain general`), and the verify-round refuter reproduced it on 9 of 35 authored human texts: careful Turkish and native-English formal WhatsApp guests (A7 class), un-flagged support-desk lines, an L2 TOEFL essay, a human forwarding an itinerary. The mechanism is SPEC §0 / §H.2–H.4 on schedule: the LLM-direction features that a formal, careful or non-native human produces for free, measured against μ/σ guessed from three telegraphic WhatsApp writers. A first draft of this ruling (proxy set = hard-mode set + the two politeness formulas) was tested against those 9 texts and would have protected 0 of 9, because `llm_lexicon_weak` (one ordinary "ayrıca"), `out_of_channel_register` (any ≥400-char two-paragraph WhatsApp message) and the prose rhythm/connector trio let every one of them escape. RULING (amends §D.3; it re-states the "≥2 features from ≥2 groups" invariant over non-proxy features):
- `REGISTER_PROXY_LLM = { terminal_punct_ratio, sentence_initial_caps, em_dash_in_chat, exclam_single_regular, emoji_bullet_led, politeness_formula, greeting_signoff_frame, tr_formal_copula, out_of_channel_register, llm_lexicon_weak }`. These may contribute to the score and the LLM channel (they still rank a queue) but never to the verdict on their own.
- `leaning_llm` requires at least TWO LLM-direction signals with `contribution > 0` that are NOT in `REGISTER_PROXY_LLM`, from at least two different groups. Otherwise the table's `leaning_llm` becomes `uncertain`, `warnings` gets `register_only_evidence`, `notes` names the proxy features that fired and how many non-proxy ones were found. Score, channels, Tier-0 rules and the human side are untouched; a rule still yields `likely_llm`.
- Consequence, stated: in single-message CHAT shape a style-only `leaning_llm` now needs a strong-lexicon hit plus a structure signal (`bold_lead_in_list`, `colon_led_list`, `balanced_contrast_frame`) or a rule. No chat-length human-vs-LLM corpus exists (SPEC §F.4), Turkish chat detection measured as format-only (AUC 0.975 → 0.450 in hard mode), and the design's own chat answer is `--aggregate`; the recall given up here was keyboard detection. In PROSE the rhythm and connector features (`sentence_len_mode_mass`, `parallel_openers`, `closing_summary_move`, `balanced_contrast_frame`, `enumerated_openers`, `hedge_density`) stay non-proxy because they survive hard mode (en:prose 150–499: AUC 0.884 → 0.857); an L2 essay built on taught connectors can therefore still lean LLM. That is the §G.2 bias, it stays documented, and the judge's C10 is its mitigation, not the CLI.
- Rejected alternatives: raising `mu` for `terminal_punct_ratio` (a guess replacing a guess; the table row `llm 0.25–0.6 × human < 0.25` still yields `leaning_llm`), narrowing `greeting_signoff_frame` alone (A7 stays at llm 0.62 without it), widening the fixture's `allowed` set (the fixture encodes the design's promise).
- selftest: A7 → `uncertain` with `register_only_evidence`; the `**Location:**` review (lexicon-strong + structure) → still `leaning_llm` without that warning; the refuter's H01/H07/H13/H15/H34 (`.scratch/refuter/texts/`, copied into the committed fixtures as head-approved authored rows) → `uncertain`; H03/H04 may stay `leaning_llm` and are recorded as the known ESL confound.

## R25. G4 (≥ 6 active features for prose) stays; the 50–119-token prose band abstains and says so
34 of the 50 authored LLM fixtures (90–110-token prose) return `insufficient_text` with G1–G3 passed and `too_few_active_features`, because the rhythm features switch on at 120–250 tokens. The measured `en:prose` 50–149 bucket says the scorer is barely useful there anyway (AUC 0.739, hard mode 0.623, TPR 3.7 % at 1.1 % FPR). RULING: do not lower G4 to score a band where the instrument is weakest. Instead: (a) README "what was not verified" and REPORT state that prose between 50 and ~120 tokens mostly abstains; (b) B2 rewrites the six prose humanization PAIRS per language (clean + `b`/`c` twin: hr, pr, em) at 160–260 tokens both sides so the §F.3 collapse assertion is measurable — same genre mix, provenance and transform recorded, row count unchanged; chat pairs stay as they are; (c) gate-fixtures reports how many pairs became measurable and the delta.

## R26. Housekeeping rulings from the head's acceptance run
- `score`, `channels.*` and `scoring.scoreUncapped` are printed at 12 decimals (contributions already are), so acceptance check 7 (`Σcontribution + b0Effective == logit(score)` within 1e-9) holds from the JSON alone; 6-decimal rounding put it at 1.3e-6.
- `run-eval.mjs --quick` writes to `eval/out/quick/` unless `--out` is given. A smoke run overwrote the release `REPORT.md` and `weights.fitted.json` once this round; the release report is regenerated by `node eval/run-eval.mjs && node eval/gate-fixtures.mjs --real 200 --append eval/out/REPORT.md`.
- `eval/out/REPORT.md` is machine-generated only. Hand-written findings (defects, the CAL network wedge, head rulings) live in `eval/README-eval.md` under "Findings log", so a regeneration cannot lose them.
- The phone-shaped-digit acceptance grep covers the deliverables (repo root minus `docs/design/`); the one tree-wide hit is a news-article id inside a URL in `docs/design/R1-research.md`, not a phone number, and it stays.
- `HANDOFF.md` is rewritten by the head at the end of the round in generic wording (no product marker strings).

## R27. `assistant_frame_leak` precision and recall fixes (refuter defects D1–D3, D5, D6, 19 misses)
The rule bypasses every gate and is the main road to `likely_llm`, so a false fire is the worst output the tool has. The refuter made it accuse a human three ways and slip past it nineteen ways. RULING:
- Quotation/discussion suppression: the cue search runs over the WHOLE document (raw), not a 100-char window, and the cue list becomes `chatgpt | claude | gemini | copilot | gpt(-\d)? | llm | chat ?bot | bot | assistant | asistan | \bAI\b | yapay zek[aâ] | dil modeli`. A cue that overlaps the matched span or sits within one token of it does not count (so "As an AI assistant, I cannot…" still fires and the rule cannot suppress itself). When suppressed: `notes` gets `possible_quotation_or_discussion: <cue>` and the rule does not fire. Precision over recall: an LLM text that both discusses AI and self-identifies is a miss we accept.
- Common-noun guard: bare `language model` no longer fires. It fires only in a self-identification frame: `(as|i'?m|i am|being|since i am) (just |merely |only )?an? (ai|large|artificial intelligence)? ?(language model|llm)`.
- Whitespace: rule patterns match on a whitespace-normalised copy of raw (`\s+` and NBSP collapsed to one space; line breaks included) and report the matched string from raw. Double spaces, a soft wrap and NBSP defeated every pattern.
- Turkish: `zek[aâ]` everywhere; add `bir (yapay zek[aâ] )?dil modeli(yim| olarak)`, `yapay zek[aâ] (modeli|asistanı)(yım|yim)?( olarak)?`, `eğitim verilerim`, `bilgi kesim tarih`, `gerçek zamanlı (erişim|veri)\w* (yok|bulunm)`.
- English: `i'?m (just |merely |only )?an? ai\b`, `i am (just |merely |only )?an? ai\b`, `my knowledge cut-?off`, `real[- ]time`, `(don'?t|do not|cannot|can'?t|am unable to) (have )?(access to|browse) (the )?(internet|web)`, `i (was|am|have been) trained on data`, `here'?s (your|a|an|the) (revised|rewritten|polished|updated) (version|draft|text)`, `i have (rewritten|revised|drafted|polished) (it|this|the)`. Every added pattern must be a first-person self-identification or a drafting frame; each ships with one positive and one human-negative selftest line (the refuter's `.scratch/refuter/leakprobe.mjs` list is the source).

- Implementation amendments accepted from FIX-B1 (2026-09-09): (i) "within one token" is implemented as "0 word tokens and ≤16 raw characters from the frame span" — the literal reading let `the bot said: As an AI…` accuse a human, and whitespace normalisation made a cue 90 spaces away look adjacent; `As an AI assistant, I cannot…` still fires. (ii) A third-person subject immediately after the frame (`…that as an AI he could not…`) suppresses it as reported speech; `As an AI, they/she/he …` is an accepted miss. (iii) Suppression is reported as note `assistant_frame_suppressed: "<match>" — possible_quotation_or_discussion: <cue>`. (iv) `gates.reason` stays `language_unknown` for unknown scripts and `unsupported_language` for Arabic-dominant text; both are equally non-bypassable by any rule (`language_gate_not_bypassable` note when a rule matched anyway).

## R28. `known_machine_marker` inside a human-written message is a hybrid, not a machine text (refuter D4)
A marker match proves that a machine-written segment is present, not that the message is machine-written. When the marker is the only Tier-0 rule and the human channel is > 0.6 (or ≥2 human-direction signals from ≥2 groups fire), the verdict is `uncertain` with `warnings: ['pasted_machine_text', 'hybrid_suspect']` and a note "machine-written segment inside a human-written message; the sender may be forwarding it". Otherwise SPEC §B.1 stands (`likely_llm` + `pasted_machine_text`). Mirrors G6.

## R29. Aggregate mode gates and features (refuter's aggregate observations)
The concatenation of a sender's turns is an artefact: its "paragraphs" are messages. RULING: `out_of_channel_register` and `paragraph_uniformity` do not run in aggregate mode. Aggregate gates are the 5-message floor plus the CHAT floors on the concatenation (100 chars, 20 tokens, 3 active features), not the prose floors — six typical WhatsApp turns (~190 chars) must produce a report, not `below_char_floor`. `likely_human` keeps its own requirements (≥3 human features incl. one expensive-to-fake, no rule, band ≥ 150 tokens).

## R30. Homoglyph and non-ASCII-Latin evasion gets a warning (refuter D8, SPEC §H.9)
A Cyrillic "а" or a fullwidth "Ｈ" inside an English word flipped `likely_llm` to `insufficient_text` with no trace. RULING: any word token that mixes scripts, or contains code points from Halfwidth/Fullwidth Forms or Mathematical Alphanumeric Symbols, adds `warnings: ['homoglyph_suspect']` with the count; rules and lexicon matching additionally run on a confusable-folded copy (a small explicit table: fullwidth → ASCII, the common Cyrillic/Greek look-alikes → Latin) so the leak rule still fires. Detection power against a real adversary stays zero and the README keeps saying so; the fix is the warning, not a defence.

## R31. Agent, RUBRIC and installer rulings from the end-to-end test
The installed agent ran for real from `~/.claude/agents/` through a headless session, resolved the tool via `LLM_DETECT_HOME`, read RUBRIC.md, applied the §E.1 table correctly on six texts and a six-row batch, and never printed a percentage on a verdict line. Defects and gaps, ruled:
- (a) `LLM_DETECT_HOME` set but invalid ⇒ the tool is MISSING. No second lookup path. The agent judges alone, capped at `leaning_*`, and says so on the CLI line. The `${LLM_DETECT_HOME:-$HOME/Desktop/llm-detect}` default applies only when the variable is unset.
- (b) A CLI `insufficient_text` row never receives a JUDGE verdict or band, in single or batch mode. At most one line, labelled "what little can be seen (not a verdict)". RUBRIC §3 and the agent file say the same thing.
- (c) Percentages: never as this text's confidence, band or probability. A published FPR / base-rate citation may appear only in CAVEATS, with its source named.
- (d) `--context` and `--channel` are passed only when the caller states them; otherwise `--context auto --channel unknown`. The CLI line prints the flags used and, on `insufficient_text`, the gate reason (`below_char_floor` vs `too_few_active_features` are different findings).
- (e) Judge-side `likely_llm` (§E.2) gains the artifact route: ONE artifact-class criterion quoted verbatim from the text (leaked assistant frame, markdown in a non-markdown channel, a configured machine marker, a near-duplicate) at ≥20 words is sufficient, mirroring the CLI's rule-gated path. Without it a fingerprinted text (CLI LL) landed on the same final as a style-only text (CLI EL), which inverts the design.
- (f) Judge-first is a protocol, not a promise: the agent writes its judgement file BEFORE invoking the CLI (procedure steps reordered; the CLI JSON is opened only after the judgement file exists).
- (g) CAVEATS labels are exactly `• length:`, `• language:`, `• writer:`, `• provenance:` (machine-parseable).
- (h) `install.sh --dry-run` never exits non-zero (the dry branch precedes the refusal branch); `--help` prints no stray script text.
- Untested this round and recorded for the next: the CONFLICT (⚠) block never rendered because no text split the two instruments.

## R32. Language ID: zero-vote Latin text is `mixed`, and ASCII-fied Turkish votes Turkish (core)
A 14-word ASCII-folded Turkish WhatsApp line came back `language.primary = "en"` at confidence 0.25 with the note `latin_subid_no_votes`. SPEC §B.9 already says the two Latin sub-IDs within one vote ⇒ `mixed`; zero votes each is within one vote. RULING: zero-vote Latin ⇒ `mixed` (script-agnostic feature set, `mixed_language_reduced_features`), never `en`. The `tr_asciified_probe` word list (icin, cok, degil, kisiyiz…), the Turkish chat slang list (naber, reis, abi, hocam…) and ASCII-fied Turkish suffix shapes (`-yiz`, `-iz`, `-siniz`, `-lar/-ler`, `-dan/-den`, `-ca/-ce`) count as Turkish votes. Folded Turkish is the documented shape of real Turkish chat and must not route into the `en` cell with English lexicons.

## R33. The verify-round texts become a committed fixture and part of the gate
`eval/fixtures/verify-round-1.jsonl`: the refuter's 35 human and 8 LLM texts (authored in-session, no corpus rows, provenance per row), each with `allowed` / `criticalFailure` per R24, R27, R28 (human rows: `insufficient_text | uncertain | leaning_human`, critical `likely_llm`; the two ESL-connector essays additionally allow `leaning_llm` and carry `note: "known §G.2 confound"`; the marker rows carry `requiresMarkers`; LLM rows forbid `likely_human`, and the two mimicry rows carry `expected_evasion: true`), plus the assistant-frame-leak probe list as rows with `expectRule: true|false`. `selftest.mjs` reads it if present (B1 ships an inline subset), `gate-fixtures.mjs` reports it, and the gate adds: zero `likely_llm` on its human rows, every `expectRule` row correct.

## R34. Second-pass leak-rule precision, silent abstention, code-switch note (from the verify-round fixture run)
- `assistant_frame_leak`, Turkish: the first-person marker is mandatory — `bir yapay zek[aâ] (modeli|asistanı)(yım|yim)` / `yapay zek[aâ] (modeli|asistanı) olarak` / `bir (yapay zek[aâ] )?dil modeli(yim| olarak)`; the bare common noun "yapay zeka asistanı" never fires. English: R27's tighter drafting frame REPLACES R1's `here('s| is) (a|an|the) (draft|revised|rewritten|polished)`; "Here is the revised itinerary my colleague sent over" is human. Both false fires came from `eval/fixtures/verify-round-1.jsonl` rows N07/N09, which stay in the gate as human-negatives.
- Every `insufficient_text` report carries a non-empty `reason`. The SPEC §D.3 bottom-left cell (llm < 0.25 and human < 0.25) reports `reason: "no_evidence_either_way"` with both channel values in a note; five rewritten email fixtures had abstained with all five gates passed and no explanation.
- `code_switch_observed` counts only English function words that are not also Turkish tokens (TR stopwords, the ASCII probe list and single letters excluded) and needs ≥3 hits and ≥10 % of tokens; an all-Turkish text had reported 24 "English function words".
- `gate-fixtures.mjs` exits 3 on a verify-round gate failure as well as a §I failure; that is the intended release signal.
- Findings recorded, not tuned: transform (b) is a measured no-op in prose on three of five pairs (its two targets are chat-only features) and transform (c) raised one Turkish email's score; the §F.3 "humanized scores lower" assertion is now measurable and partly false.

## R35. Agent report discipline, from end-to-end round 2 (the CONFLICT block rendered for the first time)
Through the installed copy, six engineered texts: the ⚠ CONFLICT block rendered correctly on the two EL×EH cells with both sides printed and no averaging; R31 (a) tool-missing (no fallback, capped `leaning_*`), (b) batch abstentions unjudged, (c) percentages, (f) judge-first all PASS; R28's hybrid marker produced the right provenance caveat. Remaining defects are report discipline, ruled:
- (a) CAVEATS labels get their OWN line in the agent file: exactly `• length:` `• language:` `• writer:` `• provenance:` and an optional fifth `• calibration:` — bullet `•`, no dash, no bold. Two of six runs wrote `- length:` / `- **length:**` after R31(g) had said the same thing inside a longer step; a parser misses a third of reports.
- (b) `[CONFLICT]` is printed ONLY in the six ⚠ cells of §E.1 — CLI LL×judge EH, LL×LH, EL×EH, EL×LH, EH×LL, LH×LL. CLI EH × judge EL is `uncertain` WITHOUT the tag. RUBRIC lists the six cells; the agent file points at that list. A batch run tagged an EH×EL row.
- (c) The CLI line always names the flags used (`context`, `channel`, `lang`, `domain`, `markers` when passed); one of five runs omitted them.
- (d) Invariant 4's reason "the text is a pasted template or machine output" applies only when the whole text, or all of it but a greeting line, is the paste; a message that merely contains one pasted line does not qualify. Single and batch runs had read this differently on the same bytes.
- (e) The judgement file is written once and never reopened; anything the agent wants to note after seeing the CLI goes to a second file, so an mtime audit of judge-first cannot give a false failure.
- (f) Between judging and invoking the CLI the agent reads only RUBRIC.md and the text — never the instrument's source (one run grepped `lib/` for "whatsapp").
- Recorded, not fixed: no run exercised `likely_llm` as a final, R31(e)'s artifact route, `register_only_evidence`, `homoglyph_suspect` or `templated_or_copied` through the agent; all six texts were Claude-authored, so the round measures the report mechanics, not accuracy.

## R36. Eval-harness review: the numbers were right where re-derived, and wrong in five places
An independent review re-derived §3 AUC/FPR/TPR/precision, §4 hard mode, §5/5b thresholds and strata, §7 leave-one-writer-out, §9 base rates and the fixture sections exactly, and confirmed mu/sigma from FIT-human only, t on VAL only, TEST-only headlines, zero duplicate straddles, chronological writer splits, sha256 of the five data files. Seventeen defects were reproduced; the head rules all of them fixed this round, and the report is regenerated afterwards:
- (a) Negative control (a) was computed over every split side (1 of 1,500); a false-positive rate is held-out only (1 of 317 = 0.3 %).
- (b) `make-splits.mjs` let the design round's per-message `data_split.json` override the persona hash for 443 generated rows, so 69 personas straddle and 18.7 % of in-house TEST llm rows share a persona with a FIT row. The holdout unit for generated rows is the persona; the recorded split is reused at GROUP level or not at all; the integrity line asserts 0 straddling non-writer groups and breaks the straddle count down by group kind (only `writer::` is excused by the chronological split). tr:chat's cell AUC moves from 0.975 to about 0.980 and its fitted cell is refitted.
- (c) `weights.fitted.json` could not be loaded by the CLI (missing per-feature `kind` and `K`; an uncaught TypeError instead of exit 4). run-eval emits every field the loader dereferences; `validateWeights` in the core requires them and exits 4 naming the missing field; a selftest loads a minimal fitted-shaped file, reports provenance `fitted`, and reports `weights_expired` for `now` past `expiresAt`. R23's opt-in path becomes real.
- (d) `corpusHash` hashes ids only; it becomes a hash of sorted (id, side, normalised-text hash).
- (e) Four of the five public files carry no `pair` key, so the matched-pair protection silently no-ops (HC3 human/ChatGPT answers to one question can straddle). `make-splits` reports pair-key coverage per source and refuses to call a source "pair-protected" without it. ONE re-pull of `hc3-en` with the current fetcher is permitted, under `timeout 900`, to recover the key; if it wedges (E3), the data of record stays and the report says the HC3 pair protection is inactive.
- (f) Control (d) shuffles sentences from the shipped segmenter (`lib/segment.mjs`), preserves inter-sentence whitespace, counts rows that became gated after the shuffle, and names the features that moved on the largest deltas.
- (g) Isotonic regression pools tied scores before PAVA; the ECE column changes (AUC/FPR/TPR/t do not).
- (h) `headline()` validates the rows it summarises (every row `side === 'test'`), not a literal; the CAL appendix is run through the same guard before it is appended. NO-COVERAGE rows print the measured confusion, never a literal 0. §7 states that the fold model pools all four cells. The LOWO fallback threshold set excludes the held-out writer. §1 prints the contamination bands from `splits-report.json`. The integrity audit uses `make-splits`' own `normKey`. `gate-fixtures.mjs`'s header matches section D (a batch run). `assertMageMapping` also asserts `maide-up-tr` and `hc3-en`.
- (i) The L2 penalty is scaled by 1/n (sklearn-style `C = 1/lambda` on the summed loss); documented in `weights.fitted.json` as `lambdaEffective`, not refitted this round.
- Outcome (FIX-B2, same day): all seventeen closed; report regenerated. Control (a) is 1 of 317 = 0.3 %; persona straddle 135 of 723 test rows → 0 of 771; the one permitted HC3 re-pull recovered the `pair` key for all 3,000 rows and showed that 434 of 500 multi-row HC3 pairs had straddled the old split — 0 now; the three review corpora still carry no pair key and are printed "not measurable — no key". Because the test set itself changed, every §3 figure moved at the third decimal (en:prose 150–499: AUC 0.884, hard 0.857, FPR 7.6 %, TPR 65.0 %; 50–149: AUC 0.739, hard 0.623, TPR 3.7 %; ECE 50–149 0.176 — the correct tie-pooled value, worse than the tie-broken 0.137, so no probability is to be quoted from that cell); tr:chat cell AUC unchanged at 0.975, hard 0.450. The reviewer's predicted tr:chat 0.980 was for deleting the leaked rows, not reassigning them, and did not reproduce. Accepted as built: the CAL-appendix guard carries a registered, disclosed per-line exemption for three leak-probe labels that quote "training"; run-eval's exit-5 leak branch is verified by code only (staging it needs a synthetic non-corpus split fixture — next round); the penalty scaling is documented as `lambdaEffective`, not refitted. `weightsId` is required by the loader and emitted as `fitted-<corpusHash8>`; a not-fitted cell re-asserts `uncalibrated_weights` on purpose.

## R37. The harness self-test is part of the release sequence, and it proves refusal, not correctness
`eval/selftest-eval.mjs` runs `run-eval.mjs` and `gate-fixtures.mjs` on a synthetic, corpus-free split fixture (`eval/fixtures/synthetic-split/`, regenerated deterministically) and asserts that the honesty guards fire on planted faults: a persona straddle → exit 5, a duplicate or both-label straddle → exit 5 (previously a warning followed by the numbers — fixed), `headline()` over a non-test row → exit 4, an unmarked fit-side number → exit 4, the CAL append guard refusing a fourth "training" wording while allowing the three registered ones, a broken fitted-weights object → exit 6 with nothing written, determinism, and the INSUFFICIENT / NO COVERAGE placeholders. RULING: (a) `package.json` gains `"selftest:eval": "node eval/selftest-eval.mjs"`; the release regeneration sequence is `node selftest.mjs && node eval/selftest-eval.mjs && node eval/run-eval.mjs && node eval/gate-fixtures.mjs --real 200 --append eval/out/REPORT.md`, and README-eval says so; (b) its default work dir is lane-neutral (`.scratch/selftest-eval/`); (c) wherever the self-test is cited, the sentence beside it is: the green checks (ten at R37, eleven since the essay check of R44) prove the guards refuse planted faults; the correctness of the numbers printed when nothing fires rests on the independent re-derivation of R36, which a fixture cannot replace; (d) the synthetic "human" class is shaped to reach code paths (capitalised, terminated sentences so G4 passes) and models nothing about human writing.

## R38. Refuter round 2 against the fixed core: the leak rule's reported-speech guard, the human checklist road, the support-desk retag
111 new authored texts and 4 aggregate files through the fixed core. No crash, no NaN, deterministic, 200k chars in 0.25 s; R29 and R32 confirmed on their targets. RULINGS:
- (a) `assistant_frame_leak` reported-speech suppression (three CRITICAL false fires on human prose, one guard): the third-person guard accepts `it|its|they|their|the|this|that|his|her` as well as `he|she`, within FOUR tokens after the frame span (`…that as an AI system he had no way…`, `…as an AI product the software could not…`); and a reporting clause in the same sentence BEFORE the frame suppresses too — EN `(said|told|explained|argued|insisted|claimed|wrote|replied|answered|responded|began|read|went)( with| that)?:?` within 12 tokens, the noun frames `the (reply|message|answer|response|email|text|bot|chat) (began|said|read|was|went)`, TR `(dedi|demiş|yazdı|yazmış|cevap verdi|yanıtladı|şöyle|şu şekilde)`. Recall cost accepted: an assistant writing `As an AI, the information I provide…` is now a miss.
- (b) Cue words that are also names or ordinary words — `claude`, `gemini`, `copilot` — count as suppression cues only within three tokens of an AI-context word (`ai|model|assistant|chatbot|bot|llm|yapay zek[aâ]|asistan`); `chatgpt`, `gpt`, `llm`, `chatbot`, `bot`, `assistant`, `AI` stay whole-document cues, all word-bounded under /u.
- (c) Twenty new self-identification patterns, first-person only, each with a positive and a human-negative selftest line: EN `being an? (ai|artificial intelligence|machine learning|language) model`, `as an? artificial intelligence`, `as a machine learning model`, `my (responses|answers|replies) are (generated|produced)`, `i (don'?t|do not) have personal (experiences|opinions|feelings|preferences)`, `i (don'?t|do not) have feelings`, `i have no (ability|way) to (access|browse)`, `i'?m not able to (browse|access) the (internet|web)`, `i was (created|developed|built|trained) by (an? )?(ai company|openai|anthropic|google|meta)`, `i am an automated (assistant|system|response)`; TR `bir yapay zek[aâ] olarak`, `ben bir yapay zek[aâ]y[ıi]m`, `kişisel deneyimim (yok|bulunm)`, `yanıtlarım otomatik olarak üretil`, `gerçek zamanlı (verilere |bilgilere )?erişimim (yok|bulunm)`, `duygularım (veya|ya da) tercihlerim yok`, `bir dil modeli olarak`.
- (d) `homoglyph_suspect` also fires on Fullwidth digits (U+FF10–FF19) and Mathematical Alphanumeric Symbols (U+1D400–1D7FF); both are folded for rule and lexicon matching (Math Alphanumerics by the block formula with its reserved holes; fullwidth digits to ASCII).
- (e) The aggregate report carries only the aggregate decision's warnings and notes plus the base pass's non-decision warnings (`uncalibrated_weights`, `weights_expired`, `expiry_not_checked`, `mixed_language_reduced_features`); `register_only_evidence`, `single_feature_guard` and table-derived notes of the discarded per-document pass are dropped; notes are de-duplicated like warnings.
- (f) The human checklist road under R24 (`- Check-in: 14:00` bullets + one strong phrase): `bold_lead_in_list` requires actual `**bold**`; the plain capitalised-lead shape moves into `colon_led_list`, and `colon_led_list` joins `REGISTER_PROXY_LLM` (SPEC calls it weak, w 0.25; humans write labelled lists all day). Measured on the refuter's 18 human-register texts, `llm_lexicon_strong` (13), `bold_lead_in_list` (11) and `colon_led_list` (10) were the road; real bold stays a signal because markdown in a non-markdown channel is the artifact the design trusts.
- (g) Support-desk retag (R7 list, second pass): the lexicon rows that carried human support-desk texts to `leaning_llm` under `--domain customer_service` are retagged `cs` so the flag zeroes them — at least `i hope this helps`, `please let me know`, `we look forward to welcoming you`, `keyifli bir tatil geçirmenizi dileriz`, `iyi tatiller dileriz`, `bizi tercih ettiğiniz için teşekkür`, and every row the refuter's A05/A13/A15/A18 reports name (`.scratch/refuter2/findings.md`). README failure mode #4's mitigation must actually fire on the failure mode.
- (h) Language ID: out-of-scope Latin languages must not become `tr`. A French formal letter came back `tr` at 0.70 because `de/ne/en/ce/la` sit on the Turkish stopword list; Azerbaijani and Turkmen came back `tr` with no note. Add an "other Latin" vote from small ASCII function-word lists for FR/ES/IT/DE/PT (10–15 words each, excluding any word on the EN or TR lists); if it is ≥ the winning EN/TR vote the result is `unknown` with note `latin_other_language` (G3, never scored). Azerbaijani `ə` (≥2) or Turkmen `ý|ž|ň` (≥2) ⇒ `unknown` with a note. English text that ties on suffix shapes stays `mixed` (accepted).
- Accepted costs, recorded: an assistant that both self-identifies and mentions "the bot"/"our assistant" elsewhere stays suppressed (R27); an LLM text carrying a marker and bought human markers (lowercase + `lol`) demotes to `uncertain` + `hybrid_suspect` (R28) — the reader still sees `pasted_machine_text`; R24's chat recall cost is real: ten clean assistant WhatsApp replies 10/10 `leaning_llm` → 0/10 after stripping bold and lowercasing four labels (7 abstain, 3 `leaning_human` — all Turkish). Written into README §H.

## R39. The agent gate (`judge-tests.jsonl`) ran for the first time: 10 PASS / 0 CRITICAL / 3 OUTSIDE, and the three are fixture errors
Through the installed copy, 14 rows: every §E.1 cell recomputed by hand matched the printed FINAL; `likely_llm` appeared as a FINAL for the first time (T06, CLI `known_machine_marker` × judge artifact route, R31(e)) and the judge withdrew a pre-CLI evidence bullet on reading `register_only_evidence` — RUBRIC §8 observed working; CAVEATS labels 13/13 exact; `[CONFLICT]` never over-fired (T08 is the EH×EL cell); no gated row was judged; judge-first verified by tool-call order 14/14; no round-1/2 defect recurred. RULINGS:
- (a) T03, T07, T11 expect `likely_llm` from style alone. Under SPEC §D.3 the FINAL reaches `likely_llm` only in the LL×LL cell, and the CLI reaches LL only through a Tier-0 rule; D2's draft predates that decision. `expectedFinal` becomes `leaning_llm | uncertain` for the three (with `likely_llm` acceptable only if a rule fires); `criticalFailure` keeps `leaning_human | likely_human`. T11's text is lengthened to ≥150 tokens (it is authored) so that its stated shape `prose` is scorable; its `criticalFailure` drops `insufficient_text`, which R25 documents as the correct answer for 50–120-token prose.
- (b) RUBRIC §4 vs §7: §4's `likely_llm` is the JUDGE's column of the table; the FINAL reaches `likely_llm` only in LL×LL, so "style alone stops at `leaning_llm`" is a statement about the FINAL. RUBRIC says so in one sentence in both places.
- (c) Flag mapping is a table, not a restatement of R31(d): RUBRIC gains §9 "Caller statements → CLI flags" (shape stated as chat/prose → `--context chat|prose`, else `auto`; channel stated → `--channel`, else `unknown`; genre stated → `--genre`, else `auto`; domain stated as a support desk → `--domain customer_service`, else `general`; markers/corpus only when a path is given) and the agent file points at it. The CLI line prints the flags ACTUALLY PASSED, copied from the command (two runs printed `domain=general` without passing it). Five of thirteen runs mapped a stated shape differently; on T11 that was the only thing between OUTSIDE and CRITICAL.
- (d) T14 needs `eval/data/human-chat.jsonl` (gitignored). It carries `requiresEvalData: true`; the gate skips it deliberately and says so; the committed-material substitute run (49 rows) met all three of its `criticalFailure` clauses and is recorded in README-eval.
- (e) Recorded: `hybrid_suspect`, `homoglyph_suspect`, `possible_quotation_or_discussion`, `templated_or_copied` and `domain_suppressed` are still unexercised through the agent; no genuine ⚠ cell arose in this round. Minor: a gated row must have NO `JUDGE:` line at all (one run printed `JUDGE: not rendered`); `--help` is not source but the agent needs no flag reference once §9 exists; the skeleton is printed plain, never inside code fences.
- Outcome (FIX-B1, 2026-09-10): (a)–(j) landed; selftest 707 assertions; CAL-E and CAL-F gates all PASS (0/33 and 0/37 human `likely_llm`, 40/40 and 37/37 leak probes, 11/11 languages); nine checklist-road rows moved to `uncertain`; the five human rows that still lean are A03/A04/A08/A10/A11, all carried by prose rhythm/connector features (the accepted §G.2 confound). Head decisions on the builder's questions: the four R38(g) phrases that are not in the lexicon are NOT added (a new `cs` row is still an LLM hit at `--domain general` and would raise the false-positive rate on the text the ruling protects); essay clichés (`it is important to note`, `önemli bir rol oynamaktadır`) stay `general`, not `cs`; fixture E11 used a math-script `r` where the marker has a `V` — a fixture defect, the row's character becomes U+1D415 (the folded-marker path is proven and asserted); recall costs recorded: `As an AI, the information I provide…` and `As an AI, they cannot…` are misses, Turkish frames rely on the reporting-verb rule only, and an adversary can restore name-cue suppression by writing "model" next to Gemini/Claude/copilot.

## R40. The fixture-gate report prints ids, never probe text; the append guard has no exemptions
The R38/R39 release regeneration stopped at the append step: verify-round-2's C10 label ("My training data does not include events after…") carried a fit-word into the CAL-F table and the closed three-line exemption of R37 did not cover it. RULING: the CAL-E and CAL-F probe tables print row id, language, expected, observed, ok/miss/false-fire and the matched rule name only — never the probe text, in the appended markdown or the standalone file; the text lives in the fixture and is looked up by id. With nothing quoted, the `quotedProbeLines` exemption is deleted and the honesty guard stays absolute. `selftest-eval` check 7 asserts that a fixture whose probe text contains "training" produces an appended section without that text, and that a hand-written fit-word line is still refused.

## R41. Owner priority 2026-09-10: English first; Turkish stays supported but gets no further work until asked
The owner: "focus english dont worry about the turkish". RULING: language scope stays EN + TR (R22 unchanged, nothing Turkish is removed and the Turkish gates keep running), but every next-round item is English-first: agent runs, refuter rounds, fixture additions and any data effort target `en:prose` and `en:chat`. Turkish numbers stay reported exactly as measured (INSUFFICIENT in every bucket, format-only signal) and no Turkish-specific feature, lexicon or corpus work is scheduled. The owner's use case as stated: "I give a document, it tells me whether it is AI-generated" — the honest answer the tool gives is one of six verdicts with quoted evidence, `likely_llm` only on a fingerprint, style alone capped at `leaning_llm`, and `insufficient_text` below 20 words / 100 characters.

## R42. The school-platform round (owner, 2026-09-10): English essays and exam answers, one student many submissions
The owner's product: an online platform collecting essays and exam answers; it must say whether a submission is AI-generated. Every student has multiple submissions. RULINGS, English only (R41):
- (a) **The platform never gets a yes/no.** The tool's output for a platform is a four-value `summary.label` derived from the verdict — `fingerprint_found` (`likely_llm`: a Tier-0 rule matched, the matched string is quoted), `ai_style_indicators` (`leaning_llm`), `no_reliable_indicators` (`uncertain`, `leaning_human`, `likely_human`), `too_short_or_no_signal` (`insufficient_text`, with the gate reason) — plus `summary.humanReviewRequired: true` on every report and `summary.caveat` carrying the base-rate sentence. A label is never a grade input; the README section "Using this in a school platform" says so with the measured table (recall 65 %, false-flag rate 7.6 % on 150–499-word English prose; precision 0.68 at a 20 % prior, 0.31 at 5 %; non-native stratum unmeasured).
- (b) **Evidence spans for a UI.** `detect()` adds `evidenceSpans[]`: `{start, end, text, source: rule|signal, name, direction}` with offsets into the RAW input for every rule match and for every lexicon/structure/orthography hit that has a literal span (rhythm features have none and say so). The agent's quoted bullets already are these spans in prose form. This is "where the tool looked", never "these sentences are AI".
- (c) **Essay preset.** `--genre essay` is accepted (prose; behaves like `email` for the greeting/sign-off frame, i.e. off); `--preset essay` expands to `--context prose --genre essay --lang en`. Nothing else changes; gates stay.
- (d) **Per-student history is the strongest lever the platform has** (README's own caveat: "calibrate per sender against that sender's own history"). `--history <prior-submissions.jsonl>` compares the new submission's feature vector with the student's prior submissions (≥2 prior documents of ≥150 tokens): a z-distance from the student's own mean beyond 2 SD on ≥3 features yields warning `style_shift_vs_history` with the features named; consistency with history adds a note and counts as ONE human-direction signal of the aggregate group (never more). History can lower a lean toward `uncertain`, never raise one; it is a note on a verdict, not a verdict. `--aggregate` floor for prose documents becomes "5 messages, or ≥3 documents of ≥150 tokens".
- (e) **Calibrate on essays.** The English numbers are measured on reviews and QA answers; the platform's genre is student essays. `fetch-public-datasets.mjs` gains a `--probe <dataset>` mode (configs, splits, fields, row counts via datasets-server — the only permitted host) and the eval lane adds the best fetchable human-student-essay vs LLM-essay resource(s) under R9's caps, splits by essay prompt, and reports an `en:prose` essay-genre row with hard mode beside it. If no such resource is fetchable, the report says the essay cell is unmeasured.
- (f) **"What a report looks like."** README gains two real reports produced through the installed agent: one authored human-style essay (`no_reliable_indicators`) and one Claude-written essay with a fingerprint (`fingerprint_found`), verbatim, with their provenance stated. The tool does not rewrite, humanize or "clean" a text and will not.
- Outcome (FIX-B1, 2026-09-10), R42(a)–(d): landed; selftest 801 assertions; gates unchanged. Field names: `summary {label, humanReviewRequired, matched, reason, caveat}`, `evidenceSpans[] {start, end, text, source, name, direction}` with `spanUnit` (`utf16`, or `utf16_joined` in aggregate mode) and `spanlessSignals[]`, `history {priorDocs, comparedFeatures, shifted[], minTokensPerDoc}`, warnings `style_shift_vs_history` / `history_insufficient`, note `consistent_with_history`, signal `history_consistency` (human, group aggregate, w 0.35, applied after the gates so another submission never helps a thin document clear G4, excluded from the `likely_human` count). Accepted as built: the history weight is a constant in `lib/history.mjs` until a fitted value exists (a weights-file entry would be a weights change); spans DEGRADE rather than guess — a non-NFC paste drops folded-derived spans into `spanlessSignals`, no wrong offset is ever emitted; `--history` costs one `detect()` per prior document, so a platform should cache prior feature vectors (library API takes texts today). `--genre essay` routes to `en:prose`, whose numbers are measured on reviews and QA answers; the essay cell is measured by the ESSAY-DATA lane or reported as unmeasured.
- Addendum (head, 2026-09-10): every text quoted or characterised in README's "What a report looks like" lives in `examples/samples/` with the exact command that reproduces the report on the shipped build; a claim about a draft that is not committed is not a claim. The docs lane's first human-register draft was reported as `ai_style_indicators` (0.533) but the committed v2 draft returns `too_short_or_no_signal` under every flag set on the shipped core, so the README paragraph is rewritten against what reproduces. `summary.label` gets its own line in the §E.3 skeleton (`LABEL: <value>` under VERDICT) — one run of two omitted it as a clause. The backend, not the file name, states the shape: README's platform section says so explicitly. `opts.history` accepts `{id, text}` rows as well as bare strings.

## R43. A non-proxy signal counts toward `leaning_llm` only when it is material (the careful-student case)
`examples/samples/student-essay-v1-tidy.txt` — a 299-token essay in a careful student's register, no assistant phrase — returns `leaning_llm` / `ai_style_indicators` (0.533) on the shipped build. R24 as written is satisfied: two non-proxy LLM signals from two groups. But the LLM channel is carried by the proxy `terminal_punct_ratio` (+0.600 of +0.921); the second non-proxy signal, `parallel_openers`, contributes +0.030 — a sliver that lets a keyboard proxy convict. This is the platform's core false-positive class (the 7.6 % measured on 150–499-word English prose, seen from inside). RULING: for the R24 requirement (≥2 non-proxy LLM-direction signals from ≥2 groups) a signal counts only if its `contribution ≥ 0.10`; immaterial signals are listed in the `register_only_evidence` note as "below the 0.10 materiality floor". Score, channels, rules and the human side are untouched. The messy draft (v2) and its apostrophe-dropped twin (v3) are unaffected. The recall cost is measured on the 50 authored LLM fixtures and the verify-round LLM rows and written into README §H; the tidy draft's own outcome becomes `uncertain` / `no_reliable_indicators`, which is the honest answer for a text whose only material LLM evidence is that its lines end with periods.
- Outcome (FIX-B1, 2026-09-10): landed with `MATERIALITY_FLOOR = 0.10` (inclusive); selftest 818 assertions; gates unchanged. Measured cost: 0 of 100 LLM fixture rows changed (llm-en 25, llm-tr 25, verify-round-1 8, verify-round-2 42), 0 human rows changed; the only row that moved is the tidy student draft (`leaning_llm` → `uncertain`, score unchanged). The floor is a constant in `lib/score.mjs`, not a weight.

## R44. The essay cell is measured; the platform quotes the essay row, and the general prose row moved
The ESSAY-DATA lane probed 21 datasets through the permitted host (every DAIGT and PERSUADE mirror is auth-gated; one candidate was case- and punctuation-normalised on both halves; one was 5 % human) and registered `dmitva/human_ai_generated_text` (school-assignment essays, human and machine halves on one row, prompt = holdout key, licence UNDECLARED through the permitted host — usable for local measurement only, no row committed). 1,500 + 1,500 rows under R9's cap; 1,206 prompts, 0 prompt/pair straddles.
- Essay row, TEST, 150–499 tokens (224 human / 248 llm; 28.4 % of essay rows gated): AUC 0.935, **hard mode 0.964** — the only cell where hard mode exceeds standard: the essay signal is rhythm and connectives, not spelling — FPR 0.4 %, TPR 28.2 % (44.0 % hard), precision 0.986; P@20 % 0.940, P@5 % 0.769, P@1 % 0.390; 2 of 325 human essays flagged at t (0.6 %). The 50–149 and 500+ essay buckets are INSUFFICIENT.
- Because essay rows join the fitting side like any public source, the general `en:prose` row moved: 150–499 AUC 0.884 → 0.878 (hard 0.859), FPR 7.6 % → 2.5 %, TPR 65.0 % → 32.5 %, precision 0.941; 50–149 AUC 0.752 / hard 0.672, FPR 0.8 %, TPR 2.8 %; coverage 63.4 % gated; LOWO R0 27 of 33 (81.8 %). The ranking barely moved; the operating point did, because the threshold is now picked on a validation side that contains 522 essays.
- RULING: (a) README's school-platform table quotes the ESSAY row first (it is the platform's genre) with the general prose row beside it, both with n, coverage and the caveats: one corpus, one unrecorded generator family, non-native/ELL stratum UNMEASURED (no L1 column; the corpora that carry `ell_status` are gated), no exam-answer corpus fetchable at all, licence undeclared. R42(a)'s quoted figures are superseded by this ruling. (b) No genre-specific threshold this round: t_essay and the cell-wide t coincided at 0.853. (c) R37 reads "eleven green checks" (the essay check joined `selftest-eval`). (d) R23 stays: the non-native stratum has 0 rows in `en:prose` 150–499. (e) Next data candidates: ArguGPT's machine half only if a licensed learner-essay human half appears; COLING-2025 outfox slice only with a paging budget beyond R9.

## R45. Platform batch API: per-row history and a reusable history profile
A platform scores thousands of submissions and holds every student's prior submissions; R42(d)'s `--history` cost one `detect()` per prior on every call. RULING (landed 2026-09-10, selftest 838): `--jsonl` rows may carry `history: [string | {id,text}]` or `historyProfile: {...}` (a row's value wins over the command-line flag); `buildHistoryProfile(texts, opts)` is exported and returns a pure, JSON-serialisable profile `{version, cell, weightsId, minTokensPerDoc, priorDocs, inputRows, skippedRows, features: {name: {mean, sd}}}` (raw SD; the 0.25 floor is applied at compare time); `opts.historyProfile` / `--history-profile <path>` replace `history` with byte-identical output; `--build-history-profile <prior.jsonl>` prints the profile and exits 0 without scoring. A profile is valid only for the cell and weights id it was built in; a mismatch yields warning `history_profile_mismatch` and NO comparison (never a silent wrong one). An empty `history: []` warns `history_insufficient`; omitting it emits no `history` block. The platform computes one profile per student when a submission is accepted and reuses it.
- Addendum to R44 (ESSAY-DATA, 2026-09-10): the non-native/ELL essay stratum was searched for, not found — 20 more probes (45 records over 41 datasets in the manifest): W&I+LOCNESS (learner essays with CEFR levels + native controls, exactly the right shape) is viewer-disabled (HTTP 501, script-based loader; its mirrors 401/404); every ELLIPSE, Feedback-Prize-ELL and PERSUADE mirror (six names) is 401; TOEFL11/ICNALE/EFCAMDAT/FCE/Lang-8 401; JFLEG is 755 single sentences with no L1. No `control` side and no control (f) were plumbed — a switch nobody can turn on would read as a control that passed. The stratum stays UNMEASURED verbatim in the README. Two routes exist and both are the owner's call: an authenticated Hugging Face pull with the owner's own free token (breaks the unauthenticated rule, not the zero-spend rule), or — better — real student essays from the owner's platform with an ELL flag, which measure the population that matters.
- Addendum (head, 2026-09-10): `--build-history-profile` scores nothing, so the uncalibrated guard does not apply to it; `buildHistoryProfile()` rounds its floats to 6 decimals in the core (R26's reason: committed profiles must not trip the phone-shaped-digit grep) and selftest asserts a rounded profile still yields a byte-identical report on the golden. Committed samples: `examples/samples/prior-profile.json`, `class-batch.jsonl` (four submissions, three students, one deliberately stale profile), scripts `examples/platform-essay.mjs` and `examples/platform-class-batch.mjs`.

## R46. End-to-end round 4 (the platform flow through the installed agent): everything ruled since R28 was exercised and held
Seven runs, all through `~/.claude/agents/`: essay + history (human → `no_reliable_indicators`, one `consistent_with_history` signal, "not a clearance"; fingerprint → `fingerprint_found`, `style_shift_vs_history` named with z-scores and cited toward "read the essay", never toward LLM), the R43 tidy draft (`uncertain`, proxies never cited), the R28 hybrid marker (`uncertain` + the provenance sentence), the R30 homoglyph (rule fired through the fold, code points named, not leaned human), the R27 quoted-discussion essay (no rule, suppression reported as a non-hit, banned inference declined by name), and a six-row class batch (one `--jsonl` pass, grouped by student, gated row unjudged, no stray `[CONFLICT]`, and two byte-identical essays under different student ids found by the agent's own hashing and reported as an observation, not a rule). Flag mapping 7 of 7 exact; `LABEL:` 12 of 12; no percentage; judge-first by tool-call order 7 of 7. RULINGS on what remained:
- (a) One §E.1 mis-application (CLI UN × judge EH printed as `leaning_human`; the cell is `uncertain`). RUBRIC §3 gains a companion list of the ASYMMETRIC cells whose result differs from "take the more confident side" — UN×EH = UN but UN×LH = EH; EH×UN = UN, LH×UN = EH; LL×IT = UN but LH×IT = EH; EL×UN = UN — and the agent file says "the table, not intuition; the asymmetric cells are listed".
- (b) `VERDICT:` is line 1 of the report. No preamble, no repo-check sentence, no working-files block before it; anything the agent wants to add goes after WHAT WOULD CHANGE THIS VERDICT under a single `NOTES` line or nowhere.
- (c) Class-wide duplicate detection is a corpus question: the platform passes the class file (and any archive of earlier submissions, `{id, sender, text}`) as `--corpus`, so `near_duplicate` fires across students (same-sender rows are skipped by the rule). `examples/platform-class-batch.mjs` passes the class file itself as the corpus by default (`--corpus <path>` to add an archive), and README's platform section says so; a duplicate is `templated_or_copied` — "not independently authored", never proof of LLM authorship (R3).
- (d) Recorded: `templated_or_copied` and `domain_suppressed` are the last two warnings unexercised through the agent (c) covers the first next time); no ⚠ cell arose naturally in rounds 3–4 — the next round engineers one deliberately; one run consulted `--help` after sealing its judgement (harmless, R39(e) stands).

## R47. A fifth platform label: `not_independently_authored`
Adding a duplicate essay to a class batch moved the original's label from `no_reliable_indicators` to `ai_style_indicators`: `near_duplicate` alone drives the verdict to `leaning_llm` (R3), and R42(a) maps `leaning_llm` to the AI-style label. A copy is a different finding from AI style and a platform must not conflate them. RULING: `summary.label = "not_independently_authored"` when `near_duplicate` is the only Tier-0 rule and the verdict rests on it (`templated_or_copied` present); `summary.matched` quotes `near_duplicate: duplicate of <id> at Jaccard J`; `summary.caveat` carries R3's sentence ("template, copy or a shared source; not proof of LLM authorship"). When another Tier-0 rule also fires, `fingerprint_found` wins. RUBRIC's "no fifth value" becomes five values; README, RUBRIC §9/platform paragraph, examples and the agent's `LABEL:` line follow. Second sentence for the platform section: a label is true relative to the corpus it was computed against — re-running a class after a late submission can move earlier labels, so store the corpus id with the label or recompute.

## R48. First public push: product-specific literals scrubbed from HEAD, not from history
The owner created a PUBLIC GitHub repository (`saityagci/llm-detect`). Before the first push the head replaced, in tracked files, the other product's name, its local directory and its bot-marker / reference-code regexes with generic wording (CLAUDE.md, HANDOFF.md, workflow-build.js, judge-tests.jsonl T06's note, HEAD-RULINGS R4/Part 2/R17/R18, SPEC §B.1/§E.5) — applying R17/R20 to the design docs, which the local repo had tolerated. Earlier commits still contain those literals and are pushed with the history; they are not secrets or personal data (a signature glyph, a reference-code prefix, a repo name), but if the owner wants them gone from the public history the repo must be rewritten (or made private) before it gets traffic — the owner's call, offered explicitly. Private data (`eval/data/`, `docs/design/private/`) never entered git.
